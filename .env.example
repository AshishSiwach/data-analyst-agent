# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# OpenAI API key, GPT-4o-mini per _docs/technology_stack.md §1.
OPENAI_API_KEY=sk-...

# Path to the DuckDB database file built by data/ingest.py.
DUCKDB_PATH=./data_analyst_agent.duckdb

# Per-session dollar budget for LLM calls. Default per _docs/scope.md.
COST_CAP_USD=0.50
